
# This script is used to set up a Docker environment for a project.
docker-compose build

# Start the Docker containers in detached mode
docker-compose up -d

# Check the status of the Docker containers
docker-compose ps



# db : mall
# user : white
# pw : 1234